// ────────────────────────────────────────────────────────────────────────────
// APP VERSION — single source of truth for the version shown in the app and in
// the "new update available" prompt.
//
// 👉 BUMP THIS on every deploy that ships changes (e.g. 1.1.0 → 1.1.1). Also
//    update the matching "version" in ngsw-config.json's `appData` so the PWA
//    update banner announces the new version number.
// ────────────────────────────────────────────────────────────────────────────
export const APP_VERSION = '1.2.175';
